import deSerialize from 'dummy/utils/de-serialize';
import { module, test } from 'qunit';

module('Unit | Utility | de serialize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = deSerialize();
  assert.ok(result);
});
